import 'package:flutter/widgets.dart';

class NoticeTile extends StatelessWidget {
  const NoticeTile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
