import 'package:flutter/widgets.dart';

class ScrollableNewsTile extends StatelessWidget {
  const ScrollableNewsTile({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}