import 'package:flutter/widgets.dart';

class ShortNewsTile extends StatelessWidget {
  const ShortNewsTile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
